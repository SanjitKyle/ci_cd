name: Exercise 5 
on:
 push:
  branches:
   - main 
 workflow_dispatch:

jobs:
 test_jobs:
  runs-on: ubuntu-latest
  steps:
    - name: Step 1 - checkout code 
      uses: actions/checkout@v4
    - name: Step 2 - Run tests
      run: |
       echo " Running test ... "
       echo " Tested Passed "
  
 deploy_jobs:
  runs-on: ubuntu-latest
  needs: test_job
  steps:
   - name : Step 1 Deploy to productions 
     run: |
      echo "Deploying application to the server... 🚀"