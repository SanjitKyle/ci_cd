name: exercise 2 Trigger and filters 
on: 
 push:
  branches:
   - main
  path-ignores:
   - '**.md'
  workflow_dispatch:

  jobs:
   filter_jobs:
     runs-on: ubuntu-


jobs:
 my_first_job:
  runs-on: ubuntu-latest 
  steps:
    - name: Step 1 -  Check out repository
      uses: actions/checkout@v4
    - name: Step 2 - Print Trigger Info
      run : |
        echo " Branch Triggers : ${{github.ref_name}}"
        echo "Event Type  : ${{github.event_name}}"
        
