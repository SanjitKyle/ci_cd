name: First yml file to test the workflow
on: 
 push:

jobs:
 my_first_job:
  runs-on: ubuntu-latest 
  steps:
    - name: Step 1 -  Check out repository
      uses: actions/checkout@v4
    - name: Step 2- Check the version of node 
      run: node -v
    - name : step 3 - print success message
      run: echo "Hellow , this is first yml file to test the workflow"
      
