name: Myworkflow
on:
  push:
   workflow_dispatch:
jobs:
 check-my-cde:
   runs-on: ubbuntu-latest
   steps:
    - name: 1. Download my code 
      uses: actions/checkout@v3
    - name: 2. Check node version
      run: node -v
    - name : 3. Print my success message
      run : echo "My workflow is sucessfully running "
