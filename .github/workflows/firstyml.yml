name: Exercise first practice 
  
on:
  push:
jobs:
  my_first_job:
    runs-on: ubuntu-latest
    
    steps: 
      - name : Step 1 Download the repository
        uses: actions/checkout@v4

      - name : Step 2 check installed verison of node 
        run : node -v 
      - name : Step 3 Print message
        run : echo "Yml is succesfully"
 
   